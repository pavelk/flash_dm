<h1>Listing slogans</h1>

<table>
  <tr>
  </tr>

<% for slogan in @slogans %>
  <tr>
    <td><%= link_to 'Show', slogan %></td>
    <td><%= link_to 'Edit', edit_slogan_path(slogan) %></td>
    <td><%= link_to 'Destroy', slogan, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New slogan', new_slogan_path %>
